{
    "version": 2,
    "builds": [
        {
            "src": "./index.js",
            "use": "@vercel/node"
        }
    ],
    "buildCommand": "npm i && npm i -g pg && npm run build",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}